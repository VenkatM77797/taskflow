import { TaskBoardPage } from '@/components/TaskBoardPage';

export default function InboxPage() {
  return <TaskBoardPage title="Inbox" subtitle="Capture everything first. Organize it later." view="inbox" />;
}
